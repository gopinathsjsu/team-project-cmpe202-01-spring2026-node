# Replaced by assemble-nginx-node-app.sh + nginx-node-app-http-base.conf,
# nginx-node-app-locations-snippet.conf, and optional nginx-node-app-443.tls.fragment.
# Keeping this stub so older docs that mention nginx-node-app.conf still find an explanation here.
